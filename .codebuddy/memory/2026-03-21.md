# 2026-03-21

## Ship VFX Phase A 治理收尾完成

### 验证结果
- Ship.prefab 通过 MCP `manage_prefabs` 直接验证：19 个根组件（含 ShipView Coordinator + 3 Workers），ShipVisual 下 7 子节点，BoostTrailRoot 下 9 子节点——全部正确
- BoostTrailRoot.prefab 验证：BoostTrailView + BoostTrailDebugManager + 9 子节点完整
- Phase A 五条治理标准全部通过

### 已知 MCP 问题
- `execute_menu_item` + `read_console` 存在时序问题：执行菜单后 Console 始终返回 0 条。多次重试（清空 Console → 执行菜单 → sleep 5-8s → 读取）均失败
- 解决方案：用 `manage_prefabs` (get_info/get_hierarchy) 直接验证 Prefab 结构，绕过 Console 读取

### 文档更新
- CanonicalSpec §3 + §7 已同步 Coordinator+Worker 架构
- AssetRegistry §2 + §3 + §8 已同步（新增 Workers/AuditTool/DebugInspector 条目 + Phase A 治理状态）
- ImplementationLog 已补全治理审计记录

## 体制外孤岛收编：ShipVisualJuice + DashAfterImageSpawner 纳入 Worker 体系

### 改造内容
- **ShipVisualJuice** → 第 4 个 Worker：移除自身 `OnEnable/OnDisable` 事件订阅（`ShipMotor.OnSpeedChanged` + `ShipDash.OnDashStarted`），改为 `Initialize()` + 被动公有方法。ShipView 新增 `ShipMotor.OnSpeedChanged` 订阅并路由
- **DashAfterImageSpawner** → ShipDashVisuals 的二级 Worker：移除自身 `OnEnable/OnDisable` 事件订阅（`ShipDash.OnDashStarted`），改为 `TriggerSpawn()` + `CancelSpawning()` 被动调用。ShipDashVisuals 在 `OnDashStarted()` 中委托调用
- **ShipView** 新增：`_juiceVisuals` + `_afterImageSpawner` 引用、`_enableJuiceVFX` toggle、`ShipMotor.OnSpeedChanged` 订阅/取消、`HandleSpeedChanged()` 路由、`ResetVFX()` 中 juice 重置
- **ShipPrefabRebuilder** 新增：`ShipVisualJuice` EnsureComponent + 接线、`DashAfterImageSpawner` 主动 EnsureComponent + 完整接线

### 结果
- 0 编译错误。所有 VFX 组件现在 100% 通过 ShipView → Worker 体系管理，零体制外孤岛
- 完整对照矩阵：5 个 Worker（BoostVisuals / HitVisuals / DashVisuals / VisualJuice / AfterImageSpawner）全部受 ShipView 路由、ResetVFX 清理、Inspector toggle 控制

## Halo 彻底删除 + Bloom/FlameCore 增强替代

### 背景
- Halo 之前 00:25 做了删除决策但未 commit，代码回退后又复活
- 这次彻底清理 + 用不同的技术方案替代

### 删除范围
- `BoostTrailView`：6 处 Halo 引用全部移除（字段/状态/Awake/OnDestroy/OnBoostStart/ResetState/TriggerActivationHalo 方法）
- `BoostTrailPrefabCreator`：Halo 子节点创建 + 接线移除
- `ShipJuiceSettingsSO`：3 字段 + 3 Getter 移除
- `ShipBoostVisuals`：Tooltip 更新

### 替代方案（不同于原始 Halo 方案，纯参数调优零新增代码）
- Bloom burst 强化：峰值 2.15→3.2、权重 0.72→0.88、attack 0.05→0.035、release 0.16→0.22
- FlameCore burst 强化：maxParticles 48→64、startSize 放大、burst count 16→22
- BoostTrailRoot 层数：8→7

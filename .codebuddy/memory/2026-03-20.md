# 2026-03-20

## Ship VFX 全层脚本覆盖完成

### 改动
- **ShipView.cs**: 扩展为全部 5 层 sprite 的统一视觉控制器。新增 HL 层 Boost ramp、Core 层 Boost ramp + 低血量警告脉冲、受击白闪（全层）、受击 i-frame 闪烁（全层）
- **ShipHealth.cs**: 重构为纯游戏逻辑（HP + 无敌帧计时 + 击退），移除了直接 SpriteRenderer 操作。视觉反馈通过 OnDamageTaken 事件委托给 ShipView
- **ShipJuiceSettingsSO.cs**: 新增 HL/Core/Hit Flash 参数段（~80 行）
- **ShipVFX_CanonicalSpec.md**: 更新 Runtime 主驱动关系

### 架构决策
- ShipHealth 不再持有任何 SpriteRenderer 引用 — 职责隔离
- ShipView 是船体精灵层唯一视觉权威（single source of truth for all 5 sprite layers）
- Core 低血量脉冲通过 PrimeTween Sequence 无限循环实现
- 受击闪烁通过 UniTask 异步循环实现

## 开发哲学规则更新 — Architecture-First

### 改动
- `CLAUDE.md` + `.trae/rules/claude.md` 同步修改 6 处
- 行为准则从"先 work 再 right 再 fast"改为"第一次就把架构做对"
- 新增开发哲学第 7 条"第一次就做对 (Get It Right the First Time)"
- Feature 工作流从 7 步扩展为 8 步（新增"架构设计"步骤）
- MVP 定义更新为"功能范围小但架构完整的版本"

### 影响
- 所有后续开发必须在编码前完成架构设计
- 一脚本一职责、一 SO 一关注点成为强制规则

## 模块架构速写规则植入

### 改动
- `CLAUDE.md` + `.trae/rules/claude.md`：Feature 工作流从 8 步扩展为 9 步，第 4 步改为"架构速写"（强制产出 `_ArchBrief.md`），新增第 8 步"更新架构文档"。追加完整的模块架构速写章节（Lv.1/Lv.2 模板 + 触发条件 + 演进规则）
- `Implement_rules.md`：新增 Section 8 通用模块规则模板（统一结构 + 质量标准 + 与架构速写联动矩阵）

### 设计决策
- 采用渐进式：Lv.1 Brief → Lv.2 Brief → 完整 CanonicalSpec + AssetRegistry
- 架构速写回答"是什么"，Implement_rules 回答"怎么改更稳"，两者互补不重复

## 模块归属决策规则

### 改动
- `CLAUDE.md` + `.trae/rules/claude.md`：在「模块架构速写」的「触发条件」前新增「模块归属判断」小节
- 4 步决策流程：核心模块表 → 命名空间归属 → 独立性测试 → 问用户
- 核心倾向：宁可归入已有模块，除非功能独立到"删掉不影响任何现有系统"

## ShipView Coordinator + Worker 重构

### 改动
- **ShipView.cs**: 从 898 行单体重写为 ~170 行轻量级 Coordinator（Presentation Layer vs Feel Layer 分离）
- **新建 3 个 Worker**: `ShipBoostVisuals.cs`（Boost VFX）、`ShipHitVisuals.cs`（受击 VFX）、`ShipDashVisuals.cs`（Dash VFX）
- **Editor 工具全部更新**: `ShipPrefabRebuilder`、`ShipBuilder`、`ShipVfxValidator` 同步支持新 Coordinator + Worker 架构
- 编译 0 错误通过

### 架构模式
- Coordinator（ShipView）只路由事件 + 分发 baseline colors
- Worker 不直接订阅事件，由 Coordinator 调用
- 无 fallback / legacy path，旧代码完全删除

# 实施计划 — SpinningTop 完整性补齐

- [ ] 1. 修复 Bug 三连：DOM 对接 + Part ID + archetype 字段
   - 统一 `screen-launch` / `screen-LAUNCH` 的 ID 映射，确保 `showScreen('LAUNCH')` 能正确显示发射屏幕
   - 修复 `showBaseSelection()` 中 `sharp_tip/flat_tip/rubber_tip` → `bb_sharp/bb_flat/bb_rubber` 的 ID 引用错误
   - 在 `ENEMIES_DB` 中为所有 12 种敌人补充正确的 `archetype` 字段（Aggressor/Survivor/Trapper）
   - _需求：1.5、2.1、3.1-3.4_

- [ ] 2. 修复玩家干预系统函数重定义冲突
   - 定位 `battleLoop` 和 `tickInterventionCooldowns` 的重复定义，合并为单一版本
   - 确保 `initIntervention()` 在战斗开始时被正确调用，Q/W/E 按键绑定正常工作
   - 验证冷却 tick 在每帧 `battleLoop` 中被正确执行
   - _需求：10.1-10.5_

- [ ] 3. 实现战斗 HUD 倒计时与超时平局机制
   - 在战斗 HUD DOM 中添加计时器元素，战斗开始时启动 180 秒倒计时
   - 实现超时判定逻辑：归零时双方各损失 1 HP，显示 "TIME OUT — DRAW" 结算 Modal
   - 倒计时 ≤30 秒时计时器变红色闪烁
   - _需求：5.1-5.4、11.1-11.2_

- [ ] 4. 实现 RPM 阶段弹飞加成与视觉效果
   - 在碰撞计算函数中接入 RPM 阶段系数：危险阶段（25%-50%）×1.2，濒死阶段（<25%）×1.5
   - 校准 `wobble` 振幅随 RPM 下降的线性增大曲线
   - RPM < 25% 时陀螺渲染颜色切换为警告红色
   - _需求：4.1-4.4_

- [ ] 5. 实现场外胜（Ring Out）和爆裂胜（Burst）判定
   - 在物理更新循环中检测敌方陀螺是否超出 `ARENA.radius`，触发场外胜（金币 +20%）
   - 在碰撞处理中检测濒死状态（RPM < 25%）的 30% 爆裂概率，触发爆裂胜（金币 +50% + 零件奖励）
   - 在 Canvas 上绘制 "RING OUT!" / "BURST!" 特效文字
   - 结算 Modal 中标注胜利类型
   - _需求：6.1-6.4、11.3_

- [ ] 6. 实现旋转方向克制系统
   - 在 `ENEMIES_DB` 中为每种敌人添加 `spin` 字段（`'L'` 或 `'R'`），玩家 SG 零件携带旋转方向属性
   - 碰撞伤害计算时读取双方旋转方向：反向 ×1.3，同向 ×0.8
   - 在准备回合情报面板中高亮显示敌方旋转方向，装配界面显示当前 SG 的旋转方向（L/R）
   - _需求：9.1-9.4_

- [ ] 7. 实现 Boss 三阶段行为切换机制
   - 在 `ENEMIES_DB` 中为 Boss 敌人添加 `isBoss: true` 标记，初始 RPM 提升 50%
   - 在 `applyEnemyAI()` 中为 Boss 添加阶段判断：RPM>60% → Aggressor，30%-60% → Survivor，<30% → Trapper
   - 阶段切换时在 Canvas 上显示阶段提示文字，Boss 击败时显示专属结算界面
   - _需求：7.1-7.5_

- [ ] 8. 实现元素攻击环持续效果系统
   - 设计通用 `StatusEffect` 数据结构（类型/强度/持续帧数/粒子颜色）
   - 实现三种元素效果：火焰（燃烧区域持续伤害）、冰霜（移速 -30% 持续 3 秒）、毒素（叠层 RPM 衰减）
   - 在 Canvas 渲染循环中绘制元素粒子效果（火=橙红，冰=青蓝，毒=绿紫）
   - 接入共振核心（`sg_resonance`）的效果延长 50% 逻辑
   - _需求：8.1-8.5_

- [ ] 9. 完善发射流程的完整交互链路
   - 确认发射屏幕 Canvas 初始化（320×320）、`launchAnimLoop` 启动、角度锁定、力道条锁定的完整流程
   - 修复角度盘点击 → 力道条步骤切换 → `executeLaunch()` 调用的事件链
   - 验证从 PREP 屏幕点击 "Fight!" → LAUNCH 屏幕 → BATTLE 屏幕的完整流程可正常运行
   - _需求：1.1-1.4_

- [ ] 10. 端到端冒烟测试与结算流程收尾
   - 验证完整游戏流程：标题 → 底座选择 → 地图 → 准备回合 → 发射 → 战斗 → 结算 → 地图（循环）
   - 确认三种胜利类型（存活胜/场外胜/爆裂胜）和超时平局的结算 Modal 均能正确显示
   - 确认 Boss 节点流程（三阶段切换 + 专属结算）可正常触发
   - _需求：1-11 全部验收标准_

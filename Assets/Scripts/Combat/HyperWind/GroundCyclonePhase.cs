namespace ProjectArk.Combat.HyperWind
{
    /// <summary>
    /// Public read-only phase enum exposed by GroundCyclone for replaceable view implementations.
    /// </summary>
    public enum GroundCyclonePhase
    {
        Spawn,
        Draw,
        Burst,
        Finished
    }
}
